{
  "name": "team-task-manager",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build:frontend": "cd taskeye-frontend && npm install --include=dev && npm run build",
    "build:backend": "cd taskeye-backend && npm install --include=dev && npm run build:prod",
    "copy:frontend": "npx cpx2 'taskeye-frontend/build/**' taskeye-backend/build/build",
    "build": "npm install --include=dev && npm run build:frontend && npm run build:backend && npm run copy:frontend",
    "start": "node taskeye-backend/build/bin/www.js",
    "migrate": "node scripts/migrate.js"
  },
  "dependencies": {
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "cpx2": "^8.0.0"
  }
}
