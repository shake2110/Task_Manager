{
  "name": "team-task-manager",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build:frontend": "cd worklenz-frontend && npm install && npm run build",
    "build:backend": "cd worklenz-backend && npm install && npm run build:prod",
    "copy:frontend": "npx cpx2 'worklenz-frontend/build/**' worklenz-backend/build/build",
    "build": "npm run build:frontend && npm run build:backend && npm run copy:frontend",
    "start": "node worklenz-backend/build/bin/www.js",
    "migrate": "node scripts/migrate.js"
  },
  "dependencies": {
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "cpx2": "^8.0.0"
  }
}
